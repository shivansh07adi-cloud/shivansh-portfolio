# Optional override for the contact-form key (default is set in src/data.ts -> CONTACT_FORM).
# Get/rotate a key at https://web3forms.com (messages go to the email the key was created for).
VITE_WEB3FORMS_KEY=
